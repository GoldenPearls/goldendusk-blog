# 그 전 react, springboot 한 프로젝트 안에 세팅 맞추기
[그 전 react, springboot 한 파일에서 작업하기](https://velog.io/@prettylee620/%ED%8C%80-%ED%94%84%EB%A1%9C%EC%A0%9D%ED%8A%B8-%EA%B3%B5%ED%86%B5-%ED%85%9C%ED%94%8C%EB%A6%BF-%EB%A7%8C%EB%93%A4%EA%B8%B0-%ED%94%84%EB%A1%A0%ED%8A%B8%EC%97%94%EB%93%9C%EC%99%80-%EB%B0%B1%EC%97%94%EB%93%9C-%ED%99%98%EA%B2%BD-%EC%84%A4%EC%A0%95%EC%9D%B8%ED%85%94%EB%A6%AC%EC%A0%9C%EC%9D%B4%EC%97%90-%EB%A6%AC%EC%95%A1%ED%8A%B8%EC%99%80-%EC%8A%A4%ED%94%84%EB%A7%81%EB%B6%80%ED%8A%B8-%EC%97%B0%EA%B2%B0%ED%95%98%EA%B8%B0)



# Mybais와 mysql 연동
## 🍀 MyBatis의 구조

![](https://velog.velcdn.com/images/prettylee620/post/00aeef69-9ace-4056-a722-74a6e4b2ebc8/image.png)


## 🍀 MyBatis + Mysql 연동하기

1. 초기 세팅해 둔 것을 준비해 둔다.
2. `application.properties`에 유저이름, 테이블명, 패스워드 mysql과 비교해서 넣기
- 위치 유저이름, 테이블명, 패스워드 꼭 비교하기!!!

```java
spring.datasource.url=jdbc:mysql://localhost:3306/festibook?&serverTimezone=UTC&autoReconnect=true&allowMultiQueries=true&characterEncoding=UTF-8
spring.datasource.username=Festibook
spring.datasource.password=f112
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

mybatis.mapper-locations=classpath:/mappers/*.xml
mybatis.type-aliases-package=com.in4mation.festibook
```

- 패키지명 : com.in4maion.festibook
- 테이블명, 유저이름, 패스워드
- 로컬에서 돌릴 때 맞춤

![](https://velog.velcdn.com/images/prettylee620/post/c5e4799f-f5d3-4de1-b0ba-e6502a5439ac/image.png)


- 유저이름 + 패스워드 설정

![](https://velog.velcdn.com/images/prettylee620/post/022d128e-3ef3-4385-b25a-4356fef50170/image.png)


- 테이블명

![](https://velog.velcdn.com/images/prettylee620/post/c44f3426-1be9-40f6-af74-b395e89b6f22/image.png)


- 권한 준 후 connect to Database

![](https://velog.velcdn.com/images/prettylee620/post/c9096979-e562-47f6-97e3-6fdc3f94a5bd/image.png)


- mapper가 있는 패키지 설정

![](https://velog.velcdn.com/images/prettylee620/post/f5511191-ef10-4d2b-abc1-00e15e5e8907/image.png)


```java

// application.properties 에!!
mybatis.mapper-locations=classpath:/mappers/*.xml
```

4. 전체 자바 패키지 예시

> 🔗 데이터 넣는 법 참고 : https://jellfyu.tistory.com/2
> 

![](https://velog.velcdn.com/images/prettylee620/post/c75bbd8d-3227-49f9-a635-5ad5d4c502cd/image.png)

5. `Mysql`에 지정한 곳에 더미 데이터(예시 데이터)와 테이블 넣어주기

```java
CREATE TABLE demo_table
(
    DEMO_ID   VARCHAR(32) NULL,
    DEMO_NAME VARCHAR(32) NULL
);

INSERT INTO demo_table VALUES("ID1", "DEMO_NAME");
INSERT INTO demo_table VALUES("ID2", "DEMO_NAME2");
INSERT INTO demo_table VALUES("ID3", "DEMO_NAME3");
```

6. DemoVo.java
- db스키마 대로 **저장할 객체를 만들고** lombok의 `@Data`를 이용해 get,set 메소드 만들어줌

```java
package com.in4mation.festibook.domain;

import lombok.Data;

@Data
public class DemoVo {
    private String demo_id;
    private String demo_name;
}
```

7. DemoMapper.java(`인터페이스`임)
- 우리가 **원하는 작업 형태**를 만들어줌

```java
package com.in4mation.festibook.repository;

import com.in4mation.festibook.domain.DemoVo;
import org.apache.ibatis.annotations.Mapper;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
@Mapper
public interface DemoMapper {
    List<DemoVo> select();
    void insert();
}
```

8. DemoMapper.xml 만들기(**이름 꼭 repository에 있는 것과 맞춰주기** 안그러면 못찾음)
- mapper namespace도 잘 맞춰주고
- domain과도 잘 연결해줄 것
- #{***} 부분은 DemoVo에 입력한 것과 이름 같게

![](https://velog.velcdn.com/images/prettylee620/post/ec447879-16ff-4aaa-bec1-aa7d64f672dd/image.png)


```java
<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.in4mation.festibook.repository.DemoMapper">

    <select id="select" resultType="com.in4mation.festibook.domain.DemoVo">
        SELECT *
        FROM demo_table
    </select>

    <insert id="insert">
        INSERT INTO demo_table(
                               DEMO_ID
                              ,DEMO_NAME
        ) VALUES(
                    #{demo_id}
                ,#{demo_name}
                )
    </insert>
</mapper>
```

9. DemoService.java

```
packagecom.in4mation.festibook.service;

import com.in4mation.festibook.domain.DemoVo;
import com.in4mation.festibook.repository.DemoMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
@Transactional
public classDemoService {
    @Autowired
private DemoMappermapper;

public List<DemoVo> select() {
return mapper.select();
    }

public void insert(DemoVo vo) {
mapper.insert();
    }
}
```

10. DemoController.java

```java
package com.in4mation.festibook.controller;

import com.in4mation.festibook.domain.DemoVo;
import com.in4mation.festibook.service.DemoService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@RestController
public class DemoController {
    @Autowired
    private DemoService demoService;

    @GetMapping("select")
    public List<DemoVo> getSelectList(){ return demoService.select(); }

    @GetMapping("insert")
    public void insertDemoVo(){
        DemoVo vo = new DemoVo();
        vo.setDemo_id("내가만든dummy_id");
        vo.setDemo_name("내가만든dummy_name");

        demoService.insert(vo);
    }
}
```

### 2. react 쪽 설정(연동 법)

1. `axios`를 연동해야 함
2. 라이브러리 설치

```java
npm install axios
```

3. 파일 만들기

![](https://velog.velcdn.com/images/prettylee620/post/f5dc6727-cc3b-4bec-a33d-f8c75fe242d4/image.png)


```java
import React, { useState, useEffect } from 'react';
import axios from 'axios';

function Community() {
    const [data, setData] = useState([]);

    useEffect(() => {
        // 데이터를 가져옵니다.
        axios.get('http://localhost:8080/select')
            .then(response => {
                setData(response.data);
            })
            .catch(error => {
                console.error("There was an error fetching the data!", error);
            });
    }, []);  // 빈 의존성 배열을 사용하여 마운트 시 한 번만 실행

    return (
        <div>
            <h1>커뮤니티페이지</h1>
            <ul>
                {data.map(item => (
                    <li key={item.demo_id}>
                        {item.demo_name}
                    </li>
                ))}
            </ul>
        </div>
    );
}

export default Community;
```

![](https://velog.velcdn.com/images/prettylee620/post/903d0050-342b-4721-b6ae-a5b80f08aa98/image.png)


잘 들어간 걸 확인했다면 잘 연동이 된 것일 것이다! 

## 🍀 문제상황

### 1. 문제 상황 : react화면에서는 안보이는데

1. ***react 폴더로 넘어와서 npm start***를 한 포트번호 3000번대에서는 데이터가 안보임
    
![](https://velog.velcdn.com/images/prettylee620/post/1b8d22f6-2039-4cf2-8c91-7c37c6f839d1/image.png)

![](https://velog.velcdn.com/images/prettylee620/post/4e28122c-9eaa-4d91-8e9c-473e6bc376e8/image.png)


### 2. 해결방안

1. 찾아보니 포트가 통합이 됐어도 각자 포트가 있기 때문에 controller나 특정 메서드에 *`@CrossOrigin(origins = "http://localhost:3000")` 를 해줘야 한다고 한다.*
2. 예시
- `클래스`전체로 (DemoController)

```java
package com.in4mation.festibook.controller;

import com.in4mation.festibook.domain.DemoVo;
import com.in4mation.festibook.service.DemoService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@CrossOrigin(origins = "http://localhost:3000")
@RestController
public class DemoController {
    @Autowired
    private DemoService demoService;

    @GetMapping("select")
    public List<DemoVo> getSelectList(){ return demoService.select(); }

    @GetMapping("insert")
    public void insertDemoVo(){
        DemoVo vo = new DemoVo();
        vo.setDemo_id("내가만든dummy_id");
        vo.setDemo_name("내가만든dummy_name");

        demoService.insert(vo);
    }
}
```

- 특정 메서드로

```java
@RestController
@CrossOrigin(origins = "http://localhost:3000")
public class DemoController {
    // ... (생략)
}
```

- **전역 CORS 설정 :** 전체 애플리케이션에 대한 CORS 설정을 원한다면, 스프링 부트의 **`WebMvcConfigurer`**를 구현하여 전역 CORS 설정을 추가

```java
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("http://localhost:3000")
                .allowedMethods("GET", "POST", "PUT", "DELETE");
    }
}
```

3. 이 또한 스프링 부트 서버는 꼭 동작하는 상태여야 한다.
- 리액트 포트 : 3000

![](https://velog.velcdn.com/images/prettylee620/post/dfa4944c-b24a-4161-99f0-3f5f13d2a96a/image.png)


- 스프링 부트 : 8080

![](https://velog.velcdn.com/images/prettylee620/post/a8e3a6f3-fe66-4376-8a89-17f5ace77137/image.png)


## 🍀 서버 동작 여부 확인

- postman에서도 서버 잘 동작하는지 확인 가능

![](https://velog.velcdn.com/images/prettylee620/post/0dfbc236-7fe8-48d4-abeb-125ebad9902b/image.png)


# 각자 작업시 폴더 이런식으로 나눠서 작업하기

## 참고사항? 예시사항?

- **각 패키지 안에** 자신이 작업하는 `기능명` 패키지 적고 그 안에서 자바 파일 작업

![](https://velog.velcdn.com/images/prettylee620/post/7fe4ad1f-8ea8-4edc-9e86-6fa50f9deb52/image.png)


- 리액트의 경우도 마찬가지 일단 예시로는 img에 login만 만들어뒀는데 components라던지 안에도 예시로 로그인이면 login 파일 만들어서 작업할 것

![](https://velog.velcdn.com/images/prettylee620/post/c6368686-4ce2-4c58-8633-1726692aa054/image.png)


- 도훈님이 주신 **자바 이름명** 참고해서 짜기!
1. 위에서 domain ⇒ dto로 변경 
2. 도훈님이 주신 거에서 mybatis의 경우 mapper나 repository 둘 중 하나 골라서 쓴다고 함… 다른 설정도 바꿔야 하니 **repository는 유지**하고 **mapper 필요없는 듯 해서 뺌**


# 공통 템플릿 공유
1. 브랜치 꼭 확인해주세요! `3-teamplates`에서 받아야 합니당
- 특정 브랜치 clone 명령어..?

```java
git clone -b {branch_name} --single-branch {저장소 URL}
ex) git clone -b javajigi --single-branch https://github.com/javajigi/java-racingcar
```

[GitHub - GoldenPearls/teamlogin at 3-templates](https://github.com/GoldenPearls/teamlogin/tree/3-templates)

![](https://velog.velcdn.com/images/prettylee620/post/499238c7-d4de-4714-804f-bd11ddc31e74/image.png)


2. 이게 패키지 내에 뭘 안넣어두면 패키지가 안올라가서 java 패키지들 안에 dummy라고 적힌 파일들 임시로 올려둠!! **작업시 삭제**

![](https://velog.velcdn.com/images/prettylee620/post/e5ac422c-0732-40a1-abd0-84102f8c5089/image.png)


## + 템플릿 공유 후 문제점
## 1. 문제 상황

1. [localhost:8080](http://localhost:8080) 뒤에 / 경로를 직접 치면 들어가지 못하는 현상
2. localhost:3000은 잘 들어가졌음
3. 새별님이 알려주셔서 알게됨 

## 2. 문제 분석

1. 그렇다면 리액트 단의 url이 못 넘어오는 현상
2. 찾아보니 첫번 째는 CORS를 설정 해줘야 한다는 것이었다.
- 이문제는 어제 Webconfig를 통해 설정해둔 것

![](https://velog.velcdn.com/images/prettylee620/post/74c00f74-4e30-4874-9acc-cf9334ef3443/image.png)


3. ****리액트 라우터와의 호환성 문제****

![](https://velog.velcdn.com/images/prettylee620/post/7efc8e24-6124-4d9c-b5ce-b9d40747d4ec/image.png)


## 3. 해결방안

![](https://velog.velcdn.com/images/prettylee620/post/62dd05dd-fc8d-42a1-9325-fd1c61c1e5cc/image.png)


```java
package com.in4mation.festibook.controller.front;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class HomeController {
    @GetMapping("/{path:[^\\.]*}")
    public String redirect() {
        return "forward:/";
    }
}
```

![](https://velog.velcdn.com/images/prettylee620/post/455dd25d-bdfb-426d-8028-629a38e05a7e/image.png)


잘 들어가 진다.

---

*[원문 보기 →](https://velog.io/@prettylee620/mybatis와-mysql-스프링부트-리액트-연동하기)*